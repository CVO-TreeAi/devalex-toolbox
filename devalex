#!/usr/bin/env python3

"""
DevAlex Agentic Toolbox
Your Personal Claude Code Supercharger

Main CLI entry point that delegates to core CLI system.
"""

import sys
import os
from pathlib import Path

# Add the core CLI to Python path
toolbox_root = Path(__file__).parent
sys.path.insert(0, str(toolbox_root / "core"))

try:
    from cli.main import main
    if __name__ == "__main__":
        main()
except ImportError as e:
    print(f"❌ DevAlex CLI Error: {e}")
    print("🔧 Try reinstalling DevAlex with: curl -sSL https://raw.githubusercontent.com/CVO-TreeAi/devalex-toolbox/main/install.sh | bash")
    sys.exit(1)
except Exception as e:
    print(f"❌ Unexpected error: {e}")
    sys.exit(1)